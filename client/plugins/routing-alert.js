import Swal from 'sweetalert2';

export default function ({ app }) {
    // app.router.beforeEach((to, from, next) => {
    //     if (process.client) {
    //         window.onbeforeunload = function (e) {
    //             return this.$t('message.are_you_sure_you_want_to_reload_page');
    //         };
    //         next();
    //     } else {
    //         next();
    //     }
    // });
}
